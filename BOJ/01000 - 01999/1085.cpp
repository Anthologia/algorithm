#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int x, y, w, h;

    cin >> x >> y >> w >> h;
    cout << x << " " << y << " " << w << " " << h << endl;
    
    
    cout << min( {1, 2, 3} );

    return 0;
}